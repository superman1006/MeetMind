---
name: write-test
description: 为一段函数 / 模块设计单元测试用例，覆盖正常路径、边界值与异常分支
---

# 单元测试设计技能

为目标函数 / 模块设计测试用例，输出一张用例表，每行包含：

- **用例名**：描述被测场景的一句话。
- **输入**：构造的入参。
- **预期**：期望的返回或副作用。
- **类型**：正常路径 / 边界值 / 异常分支。

要求：每个公开函数至少覆盖「一条正常路径 + 一条边界 + 一条异常」。
对涉及外部依赖（DB / 网络）的部分说明如何 mock，保持测试可重复、不依赖环境。
